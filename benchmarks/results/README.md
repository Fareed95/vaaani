# Benchmark artifacts

`make benchmark` writes immutable `<UTC timestamp>.json` and `.md` reports here. Chunking comparisons use `chunking-<UTC timestamp>.json`. Generated reports include their environment-facing API URL and never conflate retrieval latency with generation latency.
